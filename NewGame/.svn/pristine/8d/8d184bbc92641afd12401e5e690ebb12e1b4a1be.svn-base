@layout("/common/_container.html"){
	<!-- PAGE CONTENT BEGINS -->
<div class="row">
	<div class="col-xs-12 tb-grid">
	<form data-url="${ctxPath}/${code}/updateUserType" method="post" class="form-horizontal" role="form">
		<table class="table table-bordered">
			<tbody>
				<tr>
					<td style="vertical-align: middle !important;text-align:right;" class="bg-F5F5F5">玩家ID: </td>
					<td>${player.UserID}
						<input name="player.UserID" value="${player.UserID}" type="hidden"/>
					</td>
				</tr>
				<tr>
					<td style="vertical-align: middle !important;text-align:right;" class="bg-F5F5F5">当前类型: </td>
					<td>${player.TypeName}</td>
				</tr>
				<tr>
					<td style="vertical-align: middle !important;text-align:right;" class="bg-F5F5F5">指定新类型: </td>
					<td>
						<select id="MemberType" name="player.membertypeId">
							<option value="0">普通用户</option>
							<option value="1">至尊VIP</option>
						</select>
						&nbsp;&nbsp;&nbsp;<button class="btn-primary btn-sm" type="button" onclick="goAdd()" id="addSpecial">靓号新增</button>
					</td>
				</tr>
			</tbody>
		</table>
		</form>
	</div>
</div>
<script>
	$(function() {
		/*var userType = '${player.MemberTypeID}';
		// 获取账户类型
		new Ajax(BladeApp.ctxPath + "/config/getMemberType", function (data) {
	        if(data.code == 0 && BladeTool.isNotEmpty(data.data)) {
	        	var type = data.data;
	        	var html = '<option value="">请选择</option>';
	        	for (var i = 0; i < type.length; i++) {
					html += '<option value="' + type[i]['TypeID'] + '"';
					if(userType == type[i]['TypeID']) {
						html += ' selected="selected"';
					}
					html += '>' + type[i]['TypeName'] + '</option>';
				}
	        	$("#MemberType").html(html);
	        }
	    }).start();*/
	});
	var isAutoFlushTab = true;
    function goAdd() {
        layer.open({
            type: 2,
            title: '靓号新增',
            area: ['700px', '450px'],
            fix: false, //不固定
            maxmin: true,
            content: '${ctxPath}/specialAccounts/add'
        });
    }
</script>
		<!-- 按钮 -->
		@ include("/common/_curd/_btn.html",{btn_save:"确认修改",btn_close:"关闭"}) {}
@}